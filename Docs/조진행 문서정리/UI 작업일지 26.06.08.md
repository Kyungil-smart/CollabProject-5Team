# UI 작업 일지
작성자 : 조진행  
날짜 : 2026.06.08

---

## 완료 작업

### 스크립트 작성
- JobSliderGroupView (직원 모집 직군별 슬라이더 행 — 배리언트 5개, 회사 레벨 기반 잠금)

### 스크립트 수정
- HUDBinder — 낮/밤 전환 이벤트 연결, Canvas_DayBottom 토글, OnDestroy 구독 해제 추가. `OnNightLoading`에서 NightUI 전환 제거 (HUDPresenter.OnReportEnd에서 처리하도록 분리)
- HRPresenter — 드롭다운 구독 전부 주석 처리, GetSortedEmployees Observable 인자 제거, Bind 주석 해제, Show() 시 RefreshEmployeeManageList() 호출 추가, 마케팅/QA 잠금 처리, InitRecruitSliders / RefreshRecruitCost 추가, CompositeDisposable 슬라이더 구독 관리
- HRView — JobSliderGroupView 5개 SerializeField 추가, AllSliders 프로퍼티, SetTotalRecruitInfo() 추가, _firePanelList / _educationPanelList Awake 초기화 추가

### 설계도 업데이트
- GameScene_UI_설계_통합.md CompanyPopup 4탭 구조 확정 반영
  - 탭바 4개로 교체 (CompanyInfoTabButton / RankingTabButton / ManagementStatusTabButton / ExpansionTabButton)
  - Tab_CompanyInfo / Tab_Ranking / Tab_ManagementStatus / Tab_Expansion 계층 상세화
  - 프리팹 목록에 RankingItemView / ExpansionItemView 추가
  - 미완성 항목에서 CompanyPopup 제거

### 버그 확인 및 해결
- 스크롤뷰 내부 버튼 미작동 — 프리팹 루트 Button 컴포넌트 누락 원인 파악 및 수정
- HRView / ProjectView Awake SetActive(false) 주석 해제
- 밤 UI 두 번 눌러야 뜨는 문제 — HUDPresenter.SwitchToNight()에 CloseAllBottomPopups() + _settingsPresenter.Hide() 추가 (팀장 머지 완료)
- 보고서 종료 후 NightUI 활성화 흐름 정리 — OnNightLoading / OnReportEnd 이벤트 역할 분리
- 인사관리 팝업 열 때 직원 목록 바로 표시 안 되는 문제 — Show()에 RefreshEmployeeManageList() 추가
- 해고/교육 탭 UI 미표시 — _firePanelList / _educationPanelList 초기 활성화 처리 추가
- 드롭다운 구독 즉시 발화로 인한 NullReferenceException — 구독 주석 처리로 임시 해결

---

## 특이사항

- `OnGameSceneLoad` 추가로 씬 시작 시 `OnDay` 발화 처리됨 — HUDBinder 내 `SwitchToDay()` 직접 호출 중복이나 팀장 코드라 유지
- CompanyPopup — 데모 미포함, SO/Manager 미확정 항목은 전부 [TODO] 처리
- `GetSortedEmployees` — Observable 인자 미사용 버그 확인, 데모 이후 리팩토링 예정
- NPC `TargetDesk` Inspector 미연결 에러 — UI 범위 밖, 담당 팀원에게 전달
- `DeskInteract` Inspector 미연결 — 업무 시작 버튼 미작동, 팀장에게 전달

---

## 미완료

`설계도는 언제든지 변경될 수 있음`

- CompanyPopup 프리팹 제작 (데모 이후)

---

## 보류

- GetSortedEmployees 드롭다운 값 캐싱 리팩토링 — 데모 이후
- ReportCardView Subject 전환 — 데모 이후
- 오브젝트 풀링 전환 — 데모 이후
- CompanyPresenter SO/Manager 연결 — 데모 이후
- Canvas_Loading 텍스트 전환 (낮→밤: "사장실로 이동중~" / 밤→낮: "출근하는 중!") — 데모 미포함, 리소스 및 로딩 완료 신호 확정 후
- DoTween 연출 — 리소스 확정 후
