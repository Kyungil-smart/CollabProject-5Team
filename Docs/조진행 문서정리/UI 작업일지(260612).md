# UI 작업 일지
작성자 : 조진행  
날짜 : 2026.06.12

---

## 완료 작업

### 스크립트 작성 및 수정

- HUDView (QuestIconButton, QuestBanner 추가, ReputationLabel 제거, SetTimeLabel 파라미터 string 단일화)
- HUDPresenter (SavePresenter 연결, HR/Project 참조 방식 FindObjectOfType으로 변경)
- SettingsView (BGM/SFX 슬라이더 활성화, ON/OFF 토글 버튼, 타이틀로 이동 버튼, CloseButton 제거, ConfirmButton으로 교체)
- SettingsPresenter (토글 로직, 타이틀 이동 로직, OnConfirmClicked 닫기로 변경)
- SaveView (AutoSaveSlot / SaveSlot_1 / SaveSlot_2 고정 3개 구조, 슬롯 선택 스프라이트 스왑 Null 예외 처리)
- SavePresenter (슬롯 선택/해제 토글, AutoSaveSlot LoadButton만 활성, 빈 슬롯 SaveButton만 활성)
- DayBottomView (ProjectProgressFrame 토글/확장 제거, 진척도 1개 고정 필드로 교체)
- DayBottomPresenter (동적 생성 제거, 진척도 높은 순 1개만 직접 바인딩)
- AlertView (SynergyPopup 관련 코드 전체 제거)
- ProjectView (Panel_SlotSelect 제거, ActiveProjectLabel 추가, SynergyButton 제거, StaffCardView ButtonOverlay 구조 반영)
- ProjectPresenter (RefreshSlotSelect 제거, RefreshNewProject로 교체, 시너지 제거, StaffCardView OnInfoClicked/OnAssignClicked 연동)
- StaffCardView (StateOverlay/PlusIcon/MinusIcon 제거, ButtonOverlay 추가, SetAssignState 상태별 분기, 스프라이트 스왑 Null 예외 처리)
- QuestView (QuestDetailPopup / DailyQuestAlertPopup / QuestClearPopup 3개 팝업 구조)
- QuestPresenter (Addressables 프리팹 로드, 섹션별 재사용 풀, UniTask 2초 자동 닫힘)
- QuestItemView (Bind 단일 진입점, 완료 시 취소선 + alpha 0.5)

### 전달 사항

- HUDBinder.cs — SetTimeLabel 파라미터 변경(string 단일), SetReputationLabel 제거로 인한 수정 필요 (수정본 전달)
- DateTimeManager — `00년 00월 0주 월요일` 형식을 위한 year/month 계산 메서드 또는 데이터 추가 요청

---

## 특이사항

- SynergyItemView, ProjectSlotItemView, ProjectProgressItemView 프리팹 제거 대상
- StaffCardView ButtonOverlay 내 InfoButton/AssignButton 리소스 미확정 — 스프라이트 확정 후 교체
- SaveView 슬롯 선택 외곽선 리소스 미확정 — 스프라이트 확정 후 교체
- HUDPresenter HR/Project Presenter 참조를 FindObjectOfType으로 처리 — 추후 리팩토링 시 재검토

---

## 미완료

`설계도는 언제든지 변경될 수 있음`

- 없음

---

## 보류

- GetSortedEmployees 드롭다운 값 캐싱 리팩토링 — OBT 이전
- ReportCardView Subject 전환 — OBT 이전
- 오브젝트 풀링 전환 — OBT 이전
- Canvas_Loading 텍스트 전환 — 리소스 확정 후
- DoTween 연출 — 리소스 확정 후
- ExpansionItemView Selected 스프라이트 — 리소스 확정 후
- AlertView 참조 방식 리팩토링 (직접 SerializeField → 공통 처리) — OBT 이전
