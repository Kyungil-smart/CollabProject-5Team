# UI 작업 일지
작성자 : 조진행  
날짜 : 2026.06.11

---

## 완료 작업

### 스크립트 작성
- CompanyView (IBottomNightUI 구현, TMP_Dropdown 필터, _previousLabels 배열 방식)
- CompanyPresenter (IBottomNightUI 구현, OnFilterChanged.Skip(1) 캐싱, 증축 테이블 하드코딩)
- RankingItemView (_rankLabel 제거, RankIcon 스프라이트 교체 방식)
- ExpansionItemView (CardBackground [BTN] 구조, SizeImage, LockConditionLabel_1/2 분리)

### 설계도 업데이트 및 작업 완료
- GameScene_UI_설계_통합.md
  - Canvas_Quest 추가 (QuestDetailPopup, DailyQuestAlertPopup, QuestClearPopup)
  - Canvas_Save 추가 (AutoSaveSlot, SaveSlot_1/2 고정 3개 구조)
  - Canvas_Settings 추가 (BGM/SFX 슬라이더 + ON/OFF 토글, 타이틀로 이동 버튼)
  - DayUI QuestIconButton, QuestBanner 추가
  - QuestIconButton TopBar → DayUI로 이동
  - QuestBanner BannerFrame, QuestTypeLabel 추가
  - AutoSaveSlot / SaveSlot SavedGroup/EmptyGroup 구조로 통합
  - AutoSaveSlot EmptyGroup PlusIcon 추가
  - ScrollRect Content 컴포넌트 일람 추가 (Vertical Layout Group, Content Size Fitter 옵션 포함)
  - DailyQuestContent, StoryQuestContent, EventQuestContent Content 일람 추가
- Prefab_설계도.md
  - RankingItemView 구조 수정 (RowBackground [BTN], RankIcon 스프라이트 교체 방식)
  - ExpansionItemView 구조 수정 (CardBackground [BTN], SizeImage, LockConditionLabel_1/2, CardOutline 제거)
  - QuestItemView 추가 (Checkbox [TOGGLE] interactable = false, QuestLabel [TMP])

---

## 특이사항

- 퀘스트 오브젝트 프리팹 (탭/홀드 말풍선) — 리소스 확정 후 작업 예정
- QuestClearPopup ClearLabel — 리소스에 따라 TMP → IMG 변경 가능성 있음
- Canvas_Save 슬롯 3개 고정 구조 — 추후 다수 슬롯 지원 시 ScrollRect + 프리팹 분리로 전환
- QuestItemView Checkbox — Toggle 컴포넌트 임시 사용, 리소스 확정 후 IMG + 스프라이트 교체 방식으로 전환 예정

---

## 미완료

`설계도는 언제든지 변경될 수 있음`

- HUDView QuestIconButton, QuestBanner 스크립트 반영
- SettingsView, SettingsPresenter 개편 (BGM/SFX 슬라이더 + ON/OFF 토글, 타이틀로 이동 버튼)
- SaveView, SavePresenter 신규 작성
- DayBottomPresenter, HUDPresenter 저장 버튼 → SaveView로 교체

---

## 보류

- GetSortedEmployees 드롭다운 값 캐싱 리팩토링 — OBT 이전
- ReportCardView Subject 전환 — OBT 이전
- 오브젝트 풀링 전환 — OBT 이전
- CompanyPresenter SO/Manager 연결 — 데이터 확정 후
- Canvas_Loading 텍스트 전환 — 리소스 확정 후
- DoTween 연출 — 리소스 확정 후
- ExpansionItemView Selected 스프라이트 — 리소스 확정 후
