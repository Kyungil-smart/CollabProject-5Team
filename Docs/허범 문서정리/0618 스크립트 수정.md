## HRView.cs
```csharp
// Priaved에서  public으로 변경 (HRPresenter에서 참조하기 위해)
[SerializeField] public Button     HireButton;
```
## HRPresenter.cs

`RefreshApplicantList()`함수에서 카드를 클릭해서 고용할 직원 리스트에 담는 걸 잘 못하겠어서 위에 `HireButton`누르면 고용할 직원 리스트에 담음

`RefreshApplicantDetail()`에서 채용될 녀석들을 채용가능한지 검사하고 채용함

`OnFinalHireClicked()`대신 `OnFinalHireLogic()`함수 내용을 추가했는데 대체가가능할지 모르겠어서 놔뒀습니다.