# DateTimeManager.cs
- `OnWeekStarted`액션 이벤트 추가 월요일 아침이 시작될 때 호출

# _EmployeeManager.cs
- `currentApplicants` 변수 추가: 현재 지원자를 알기 위해
- `GenerateWeeklyAppicants()` 추가: 월요일이 시작될 때 채용 요청에 맞춰 유저가 선택한 직군이 새로운 지원자 리스트에 생성 `OnWeekStarted` 참조
- `RemoveFromApplicants` 추가: 고용되면 즉시 지원자 데이터 삭제

# HRPresenter.cs
- `RefreshAppicantList()` 수정: 기존 고용되지 않은 직원을 불러오던 로직을 선택한 직군만 불러오도록 함
- `OnFinalHireLogic()` 수정: 고용한 NPC는 즉시 UI에서 사라지도록
- `ResetOnMondayUI()` 추가: 월요일 낮에 고용쪽 UI가 초기화 되도록 함

# GameManager.cs
- `_isUpgradeReserved` 플래그를 세움(맵 증축 시 즉각 적용되지 않도록)
- `ReserveOfficeUpgrade()` 함수를 호출하면 맵 증축 예약(`_isUpgradeReserved`가 True로 바뀜)
- `TryProcessUpgradeAsync()`에서 `UpgradeOfficeAsync()`를 호출해 업그레이드시도(DateTimeManager.cs에 월요일이 되는 로직에서 호출)